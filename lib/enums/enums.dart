enum ImageLoaderType { user, image }

enum LoadMoreType { isLoading, idle }

enum ButtonType { normal, iconButton }

enum ImageSide { front, back }

enum PaymentCardErrorPos { empty, name, number, cvv, date }
